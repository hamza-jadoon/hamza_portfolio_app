name: hamza_portfolio_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Cupertino icons for iOS style icons
  cupertino_icons: ^1.0.8

  # State management
  provider: ^6.0.5
  http: ^1.5.0

  # For using SVG assets
  flutter_svg: ^2.0.10+1
  vector_graphics: ^1.1.11+1
  url_launcher: ^6.3.0
  custom_navigation_bar: ^0.8.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true   # ✅ Correct placement here!

  assets:
    - assets/images/
    - assets/app_icons/
    # - images/a_dot_ham.jpeg
